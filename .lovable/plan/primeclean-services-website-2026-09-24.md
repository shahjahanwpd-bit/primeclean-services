# PrimeClean Services website

## Scope
- Replace the empty starter screen with the complete PrimeClean Services website described in the uploaded brief.
- Use the supplied business copy, five service prices, contact details, and Winnipeg service area exactly.
- Remove the stale `src/App.tsx` build failure by keeping the app on its existing TanStack route structure; no broken `App.tsx` dependency will remain.

## Pages and interactions
- Build the home page with the service-focused opening slider, trust points, specialties, pricing cards, before/after proof, value points, reviews, articles, and validated quote form.
- Add About, Services, five service-detail pages, Gallery, Blog, Contact, account, booking, and legal pages so every navigation item works.
- Add a sticky desktop header, mobile menu, mobile call/booking bar, service dropdown, gallery filtering/lightbox, FAQ accordions, and inline form success feedback.

## Visual direction
- Create a crisp, premium cleaning-service design with off-white surfaces, restrained shadows, 8–12px corners, and an accessible sans-serif type system.
- Build a recognizable PrimeClean wordmark and use a tightly controlled blue/teal cleaning palette because no separate logo image was included with the uploaded brief.
- Generate cohesive cleaning imagery for the main visual areas instead of using placeholders or external hotlinks.

## Technical details
- Define the complete semantic color and typography system centrally in the global stylesheet.
- Use TanStack file routes and shared site components; add unique search/social metadata to every content route.
- Verify the production build, current diagnostics, desktop layout, mobile layout, navigation, and quote submission flow.
